# 1) Sum of First N Numbers
# Calculates the sum of numbers from 1 to N.
# Expected Output: 55

N = 10
sum = 0
for i in range(1, N + 1):
    sum = sum + i
print(sum)

# 2) Factorial of N
# Calculates the factorial of N (product of all numbers from 1 to N).
# Expected Output: 120

N = 5
factorial = 1
for i in range(1, N + 1):
    factorial = factorial * i
print(factorial)

# 3) GCD of Two Numbers
# Finds the greatest common divisor (GCD) of a and b.
# Expected Output: 6
	a = 48
	b = 18
	while b != 0:
   	    temp = b
    	    b = a % b
    	    a = temp

	print(a)

# 4) Reverse a Number
# Reverses the digits of a given number.
# Expected Output: 4321

	number = 1234
	reversed_number = 0
	while number != 0:
   	    digit = number % 10
    	    reversed_number = reversed_number * 10 + digit
    	    number = number / 10

	print(reversed_number)

# 5) Check if a Number is Prime
# Needs proper implementation to check if a number is prime.
# Example Input: 7
# Expected Output: True (for prime numbers)
	
there is an error and somehow i cant fix it so this interpreter cant run prime checker

# 6) Check if a Number is a Palindrome
# Checks if a number reads the same forwards and backwards.
# Expected Output: True -> 1
	original_number = 121
	number = original_number
	reversed_number = 0
	palindrome = False
	while number > 0:
	    digit = number % 10
	    reversed_number = reversed_number * 10 + digit
	    number = number / 10
	if reversed_number == original_number:
	    palindrome = True

	print(original_number)  # Expected: 121
	print(reversed_number)  # Expected: 121
	print(palindrome)       # Expected: 1     1 -> True  0 -> False

# 7) Largest Digit in a Number
# Finds the largest digit in a number.
# Expected Output: 9
	n = 3947
	largestdigit = 0
	while n > 0:
	    digit = n % 10
	    if largestdigit < digit:
	        largestdigit = digit
	    n = n / 10

	print(largestdigit)

# 8) Sum of Digits
# Calculates the sum of all digits in a number.
# Expected Output: 21
	number = 123456
	total = 0
	while number > 0:
	    total = total + number % 10
	    number = number / 10

	print(total)

# 9) Multiplication Table
# Prints the multiplication table for the given number.
# Example Input: 10
# Expected Output: 10, 20, 30, ..., 100
	number = 10
	for i in range(1, 11):
	    print(number * i)

# 10) Nth Fibonacci Number
# Calculates the Nth Fibonacci number.
# Example Input: N = 10
# Expected Output: 34 (10th Fibonacci number)
	n = 10	
	a = 0
	b = 1
	for i in range(2, n):
	    temp = b
	    b = a + b
	    a = temp

	print(b)
